const config = {
  db: {
    /* don't expose password or any sensitive info, done only for demo */
    host: "localhost",
    user: "shakil",
    password: "Shakil2023",
    database: "webapi",
  },
};
module.exports = config;
